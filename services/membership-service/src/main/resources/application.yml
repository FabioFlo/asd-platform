spring:
  application:
    name: membership-service

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/membership_db
    username: ${DB_USER:asd}
    password: ${DB_PASS:asd}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: ${KAFKA_SERVERS:localhost:9092}

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

logging:
  level:
    it.asd: INFO
    org.springframework.kafka: WARN
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/membership-service.log

services:
  identity:
    base-url: ${IDENTITY_SERVICE_URL:http://localhost:8082}
  registry:
    base-url: ${REGISTRY_SERVICE_URL:http://localhost:8081}
